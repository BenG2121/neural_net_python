language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - cd unittest
  - python -m unittest test_neuralNetwork.py
  - python -m unittest test_neuralNetwork.TestNeuralNetworkInit.test_update_neural_network_against_values
